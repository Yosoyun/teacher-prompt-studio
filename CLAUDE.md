This repo has no AGENTS.md — read README.md (product truth), STATE.md (where we are) and BRANCHES.md (which branch is real; the remote is named `github`, not `origin`).
This file is a pointer only; it carries no rules of its own.
